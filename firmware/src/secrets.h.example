/**
 * ESP32 Live Portfolio - Secrets Template
 * Copy this to secrets.h and fill in your credentials
 */

#ifndef SECRETS_H
#define SECRETS_H

// WiFi credentials
#define WIFI_SSID "your_wifi_ssid"
#define WIFI_PASS "your_wifi_password"

// Live Hub API URL (Cloudflare Worker)
#define LIVE_HUB_URL "https://api.blackroad.io/v1/live-hub"

// Optional: OpenWeatherMap API key (for weather screen)
#define OPENWEATHER_API_KEY "your_openweather_api_key"

// Optional: GitHub Personal Access Token (for enhanced rate limits)
#define GITHUB_TOKEN "your_github_pat"

#endif
